{
  "name": "population-management-system",
  "version": "1.0.0",
  "main": "app.js",
  "author": "HawiCaesar",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^7.0.0",
    "express": "^4.17.1",
    "mongodb": "^3.3.2",
    "mongoose": "^5.7.5",
    "morgan": "^1.9.1"
  },
  "scripts": {
    "start": "nodemon server.js",
    "test": "export NODE_ENV=test && jest tests/*.test.js --detectOpenHandles --forceExit",
    "coverage": "yarn test --coverage",
    "coverage-report": "yarn test --coverage && cat coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "coveralls": "^3.0.7",
    "jest": "^24.9.0",
    "nodemon": "^1.19.3",
    "supertest": "^4.0.2"
  }
}
